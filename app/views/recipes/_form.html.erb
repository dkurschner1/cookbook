<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="title">
    <%= f.label :title %><sup>*</sup><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :ingredients %><sup>*</sup><br>
    <%= f.text_area :ingredients %>
  </div>
  <div class="field">
    <%= f.label :instructions %><sup>*</sup><br>
    <%= f.text_area :instructions %>
  </div>
  <div class="field">
    <%= f.label :calories %><sup>*</sup><br>
    <%= f.text_area :calories %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.select :category_id, @categories, :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <div class="required">
    <p><sup>*</sup>Required</p>
  </div>

<% end %>
